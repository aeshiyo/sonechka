-- SOFIYA <3 - COMPLETE EDITION (FIXED EMPTY MENU BUG)
-- Работает на: Hydrogen, Delta, Arceus X, CodeX, Synapse, Krnl, Fluxus

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local Lighting = game:GetService("Lighting")
local CoreGui = game:GetService("CoreGui")
local Workspace = game:GetService("Workspace")

local lplr = Players.LocalPlayer
local character = lplr.Character or lplr.CharacterAdded:Wait()
local humanoid = character:WaitForChild("Humanoid")
local rootPart = character:WaitForChild("HumanoidRootPart")

lplr.CharacterAdded:Connect(function(char)
	character = char
	humanoid = character:WaitForChild("Humanoid")
	rootPart = character:WaitForChild("HumanoidRootPart")
end)

-- Удаляем старый GUI если есть
local guiName = "SofiyaGUI"
pcall(function()
	if CoreGui:FindFirstChild(guiName) then
		CoreGui[guiName]:Destroy()
	end
end)
pcall(function()
	if lplr.PlayerGui:FindFirstChild(guiName) then
		lplr.PlayerGui[guiName]:Destroy()
	end
end)

-- СОЗДАЁМ GUI
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = guiName
ScreenGui.ResetOnSpawn = false
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

pcall(function()
	if syn and syn.protect_gui then
		syn.protect_gui(ScreenGui)
		ScreenGui.Parent = CoreGui
	else
		ScreenGui.Parent = CoreGui
	end
end)
if not ScreenGui.Parent then
	ScreenGui.Parent = lplr:WaitForChild("PlayerGui")
end

-- ГЛАВНАЯ РАМКА (ОСНОВА)
local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0, 320, 0, 480)
MainFrame.Position = UDim2.new(0.5, -160, 0.5, -240)
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 25)
MainFrame.BackgroundTransparency = 0  -- НЕ ПРОЗРАЧНЫЙ!
MainFrame.BorderSizePixel = 0
MainFrame.Visible = true
MainFrame.ClipsDescendants = true
MainFrame.Parent = ScreenGui

-- СКРУГЛЕНИЕ
local Corner = Instance.new("UICorner")
Corner.CornerRadius = UDim.new(0, 16)
Corner.Parent = MainFrame

-- ===== 1. ЗАГОЛОВОК =====
local Title = Instance.new("TextLabel")
Title.Name = "Title"
Title.Size = UDim2.new(1, 0, 0, 45)
Title.Position = UDim2.new(0, 0, 0, 0)
Title.BackgroundColor3 = Color3.fromRGB(40, 0, 60)
Title.BackgroundTransparency = 0
Title.BorderSizePixel = 0
Title.Text = " SOFIYA <3"
Title.TextColor3 = Color3.fromRGB(255, 100, 200)
Title.TextSize = 24
Title.Font = Enum.Font.GothamBold
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.Visible = true
Title.Parent = MainFrame

-- ===== 2. КНОПКА ЗАКРЫТИЯ ✕ =====
local CloseBtn = Instance.new("TextButton")
CloseBtn.Name = "CloseBtn"
CloseBtn.Size = UDim2.new(0, 35, 0, 35)
CloseBtn.Position = UDim2.new(1, -42, 0, 5)
CloseBtn.BackgroundColor3 = Color3.fromRGB(60, 0, 0)
CloseBtn.BackgroundTransparency = 0
CloseBtn.BorderSizePixel = 0
CloseBtn.Text = "✕"
CloseBtn.TextColor3 = Color3.fromRGB(255, 50, 50)
CloseBtn.TextSize = 20
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.Visible = true
CloseBtn.Parent = MainFrame

local CloseCorner = Instance.new("UICorner")
CloseCorner.CornerRadius = UDim.new(0, 8)
CloseCorner.Parent = CloseBtn

CloseBtn.MouseButton1Click:Connect(function()
	ScreenGui:Destroy()
end)

-- ===== 3. КНОПКА СВЁРТЫВАНИЯ ➖ =====
local MinBtn = Instance.new("TextButton")
MinBtn.Name = "MinBtn"
MinBtn.Size = UDim2.new(0, 35, 0, 35)
MinBtn.Position = UDim2.new(1, -80, 0, 5)
MinBtn.BackgroundColor3 = Color3.fromRGB(60, 60, 0)
MinBtn.BackgroundTransparency = 0
MinBtn.BorderSizePixel = 0
MinBtn.Text = "➖"
MinBtn.TextColor3 = Color3.fromRGB(255, 200, 50)
MinBtn.TextSize = 20
MinBtn.Font = Enum.Font.GothamBold
MinBtn.Visible = true
MinBtn.Parent = MainFrame

local MinCorner = Instance.new("UICorner")
MinCorner.CornerRadius = UDim.new(0, 8)
MinCorner.Parent = MinBtn

local isMinimized = false
MinBtn.MouseButton1Click:Connect(function()
	isMinimized = not isMinimized
	if isMinimized then
		MainFrame.Size = UDim2.new(0, 320, 0, 45)
		MinBtn.Text = "➕"
	else
		MainFrame.Size = UDim2.new(0, 320, 0, 480)
		MinBtn.Text = "➖"
	end
end)

-- ПЕРЕТАСКИВАНИЕ МЕНЮ
local function MakeDraggable(gui, dragPart)
	local dragging, dragInput, dragStart, startPos
	dragPart.InputBegan:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
			dragging = true
			dragStart = input.Position
			startPos = gui.Position
			input.Changed:Connect(function()
				if input.UserInputState == Enum.UserInputState.End then dragging = false end
			end)
		end
	end)
	dragPart.InputChanged:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
			dragInput = input
		end
	end)
	UserInputService.InputChanged:Connect(function(input)
		if input == dragInput and dragging then
			local delta = input.Position - dragStart
			gui.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
		end
	end)
end
MakeDraggable(MainFrame, Title)

-- ===== 4. FLY КНОПКА =====
local FlyBtn = Instance.new("TextButton")
FlyBtn.Name = "FlyBtn"
FlyBtn.Size = UDim2.new(0.75, 0, 0, 45)
FlyBtn.Position = UDim2.new(0.05, 0, 0.12, 0)
FlyBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 50)
FlyBtn.BackgroundTransparency = 0
FlyBtn.BorderSizePixel = 0
FlyBtn.Text = "🚀 FLY [ВЫКЛ]"
FlyBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
FlyBtn.TextSize = 18
FlyBtn.Font = Enum.Font.GothamBold
FlyBtn.Visible = true
FlyBtn.Parent = MainFrame
Instance.new("UICorner", FlyBtn).CornerRadius = UDim.new(0, 8)

-- ===== 5. ПОЛЗУНОК FLY =====
local FlySlider = Instance.new("Frame")
FlySlider.Name = "FlySlider"
FlySlider.Size = UDim2.new(0.6, 0, 0, 20)
FlySlider.Position = UDim2.new(0.05, 0, 0.22, 0)
FlySlider.BackgroundColor3 = Color3.fromRGB(50, 50, 70)
FlySlider.BackgroundTransparency = 0
FlySlider.Visible = true
FlySlider.Parent = MainFrame
Instance.new("UICorner", FlySlider).CornerRadius = UDim.new(0, 4)

local FlySliderBtn = Instance.new("TextButton")
FlySliderBtn.Name = "FlySliderBtn"
FlySliderBtn.Size = UDim2.new(0.2, 0, 1.2, 0)
FlySliderBtn.Position = UDim2.new((50-10)/(200-10), -10, -0.1, 0)
FlySliderBtn.BackgroundColor3 = Color3.fromRGB(139, 92, 246)
FlySliderBtn.BackgroundTransparency = 0
FlySliderBtn.BorderSizePixel = 0
FlySliderBtn.Text = ""
FlySliderBtn.Visible = true
FlySliderBtn.Parent = FlySlider
Instance.new("UICorner", FlySliderBtn).CornerRadius = UDim.new(1, 0)

-- ===== 6. ЦИФРА СКОРОСТИ FLY =====
local FlySpeedText = Instance.new("TextLabel")
FlySpeedText.Name = "FlySpeedText"
FlySpeedText.Size = UDim2.new(0.15, 0, 0, 20)
FlySpeedText.Position = UDim2.new(0.7, 0, 0.22, 0)
FlySpeedText.BackgroundColor3 = Color3.fromRGB(30, 30, 50)
FlySpeedText.BackgroundTransparency = 0
FlySpeedText.BorderSizePixel = 0
FlySpeedText.Text = "50"
FlySpeedText.TextColor3 = Color3.fromRGB(255, 255, 255)
FlySpeedText.TextSize = 16
FlySpeedText.Font = Enum.Font.GothamBold
FlySpeedText.Visible = true
FlySpeedText.Parent = MainFrame
Instance.new("UICorner", FlySpeedText).CornerRadius = UDim.new(0, 4)

-- ===== 7. КНОПКА "+" ДЛЯ FLY =====
local PlusFly = Instance.new("TextButton")
PlusFly.Name = "PlusFly"
PlusFly.Size = UDim2.new(0, 34, 0, 34)
PlusFly.Position = UDim2.new(0.85, 0, 0.12, 5)
PlusFly.BackgroundColor3 = Color3.fromRGB(34, 197, 94)
PlusFly.BackgroundTransparency = 0
PlusFly.BorderSizePixel = 0
PlusFly.Text = "+"
PlusFly.TextColor3 = Color3.fromRGB(255, 255, 255)
PlusFly.TextSize = 22
PlusFly.Font = Enum.Font.GothamBold
PlusFly.Visible = true
PlusFly.Parent = MainFrame
Instance.new("UICorner", PlusFly).CornerRadius = UDim.new(1, 0)

-- ===== 8. NOCLIP КНОПКА =====
local NoclipBtn = Instance.new("TextButton")
NoclipBtn.Name = "NoclipBtn"
NoclipBtn.Size = UDim2.new(0.75, 0, 0, 45)
NoclipBtn.Position = UDim2.new(0.05, 0, 0.28, 0)
NoclipBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 50)
NoclipBtn.BackgroundTransparency = 0
NoclipBtn.BorderSizePixel = 0
NoclipBtn.Text = "👻 NOCLIP [ВЫКЛ]"
NoclipBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
NoclipBtn.TextSize = 18
NoclipBtn.Font = Enum.Font.GothamBold
NoclipBtn.Visible = true
NoclipBtn.Parent = MainFrame
Instance.new("UICorner", NoclipBtn).CornerRadius = UDim.new(0, 8)

-- ===== 9. КНОПКА "+" ДЛЯ NOCLIP =====
local PlusNoclip = Instance.new("TextButton")
PlusNoclip.Name = "PlusNoclip"
PlusNoclip.Size = UDim2.new(0, 34, 0, 34)
PlusNoclip.Position = UDim2.new(0.85, 0, 0.28, 5)
PlusNoclip.BackgroundColor3 = Color3.fromRGB(34, 197, 94)
PlusNoclip.BackgroundTransparency = 0
PlusNoclip.BorderSizePixel = 0
PlusNoclip.Text = "+"
PlusNoclip.TextColor3 = Color3.fromRGB(255, 255, 255)
PlusNoclip.TextSize = 22
PlusNoclip.Font = Enum.Font.GothamBold
PlusNoclip.Visible = true
PlusNoclip.Parent = MainFrame
Instance.new("UICorner", PlusNoclip).CornerRadius = UDim.new(1, 0)

-- ===== 10. SPEED КНОПКА =====
local SpeedBtn = Instance.new("TextButton")
SpeedBtn.Name = "SpeedBtn"
SpeedBtn.Size = UDim2.new(0.75, 0, 0, 45)
SpeedBtn.Position = UDim2.new(0.05, 0, 0.40, 0)
SpeedBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 50)
SpeedBtn.BackgroundTransparency = 0
SpeedBtn.BorderSizePixel = 0
SpeedBtn.Text = "⚡ SPEED [ВЫКЛ]"
SpeedBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
SpeedBtn.TextSize = 18
SpeedBtn.Font = Enum.Font.GothamBold
SpeedBtn.Visible = true
SpeedBtn.Parent = MainFrame
Instance.new("UICorner", SpeedBtn).CornerRadius = UDim.new(0, 8)

-- ===== 11. ПОЛЗУНОК SPEED =====
local SpeedSlider = Instance.new("Frame")
SpeedSlider.Name = "SpeedSlider"
SpeedSlider.Size = UDim2.new(0.6, 0, 0, 20)
SpeedSlider.Position = UDim2.new(0.05, 0, 0.50, 0)
SpeedSlider.BackgroundColor3 = Color3.fromRGB(50, 50, 70)
SpeedSlider.BackgroundTransparency = 0
SpeedSlider.Visible = true
SpeedSlider.Parent = MainFrame
Instance.new("UICorner", SpeedSlider).CornerRadius = UDim.new(0, 4)

local SpeedSliderBtn = Instance.new("TextButton")
SpeedSliderBtn.Name = "SpeedSliderBtn"
SpeedSliderBtn.Size = UDim2.new(0.2, 0, 1.2, 0)
SpeedSliderBtn.Position = UDim2.new((40-16)/(120-16), -10, -0.1, 0)
SpeedSliderBtn.BackgroundColor3 = Color3.fromRGB(245, 158, 11)
SpeedSliderBtn.BackgroundTransparency = 0
SpeedSliderBtn.BorderSizePixel = 0
SpeedSliderBtn.Text = ""
SpeedSliderBtn.Visible = true
SpeedSliderBtn.Parent = SpeedSlider
Instance.new("UICorner", SpeedSliderBtn).CornerRadius = UDim.new(1, 0)

-- ===== 12. ЦИФРА СКОРОСТИ SPEED =====
local SpeedText = Instance.new("TextLabel")
SpeedText.Name = "SpeedText"
SpeedText.Size = UDim2.new(0.15, 0, 0, 20)
SpeedText.Position = UDim2.new(0.7, 0, 0.50, 0)
SpeedText.BackgroundColor3 = Color3.fromRGB(30, 30, 50)
SpeedText.BackgroundTransparency = 0
SpeedText.BorderSizePixel = 0
SpeedText.Text = "40"
SpeedText.TextColor3 = Color3.fromRGB(255, 255, 255)
SpeedText.TextSize = 16
SpeedText.Font = Enum.Font.GothamBold
SpeedText.Visible = true
SpeedText.Parent = MainFrame
Instance.new("UICorner", SpeedText).CornerRadius = UDim.new(0, 4)

-- ===== 13. КНОПКА "+" ДЛЯ SPEED =====
local PlusSpeed = Instance.new("TextButton")
PlusSpeed.Name = "PlusSpeed"
PlusSpeed.Size = UDim2.new(0, 34, 0, 34)
PlusSpeed.Position = UDim2.new(0.85, 0, 0.40, 5)
PlusSpeed.BackgroundColor3 = Color3.fromRGB(34, 197, 94)
PlusSpeed.BackgroundTransparency = 0
PlusSpeed.BorderSizePixel = 0
PlusSpeed.Text = "+"
PlusSpeed.TextColor3 = Color3.fromRGB(255, 255, 255)
PlusSpeed.TextSize = 22
PlusSpeed.Font = Enum.Font.GothamBold
PlusSpeed.Visible = true
PlusSpeed.Parent = MainFrame
Instance.new("UICorner", PlusSpeed).CornerRadius = UDim.new(1, 0)

-- ===== 14. BUST FPS КНОПКА =====
local BustFPSBtn = Instance.new("TextButton")
BustFPSBtn.Name = "BustFPSBtn"
BustFPSBtn.Size = UDim2.new(0.75, 0, 0, 45)
BustFPSBtn.Position = UDim2.new(0.05, 0, 0.58, 0)
BustFPSBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 50)
BustFPSBtn.BackgroundTransparency = 0
BustFPSBtn.BorderSizePixel = 0
BustFPSBtn.Text = "🎮 BUST FPS [ВЫКЛ]"
BustFPSBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
BustFPSBtn.TextSize = 18
BustFPSBtn.Font = Enum.Font.GothamBold
BustFPSBtn.Visible = true
BustFPSBtn.Parent = MainFrame
Instance.new("UICorner", BustFPSBtn).CornerRadius = UDim.new(0, 8)

-- ===== 15. КНОПКА СБРОСА =====
local ResetBtn = Instance.new("TextButton")
ResetBtn.Name = "ResetBtn"
ResetBtn.Size = UDim2.new(0.9, 0, 0, 45)
ResetBtn.Position = UDim2.new(0.05, 0, 0.70, 0)
ResetBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
ResetBtn.BackgroundTransparency = 0
ResetBtn.BorderSizePixel = 0
ResetBtn.Text = "СБРОС ВСЕХ НАСТРОЕК"
ResetBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
ResetBtn.TextSize = 18
ResetBtn.Font = Enum.Font.GothamBold
ResetBtn.Visible = true
ResetBtn.Parent = MainFrame
Instance.new("UICorner", ResetBtn).CornerRadius = UDim.new(0, 8)

-- ===== ЛОГИКА СЛАЙДЕРОВ =====
local States = { Fly = false, Noclip = false, Speed = false, FPS = false }
local Values = { Fly = 50, Speed = 40 }
local FloatingFrames = {}
local FloatingPositions = {}
local NoclipParts = {}
local OldLighting = {}
local bg, bv

local function SetupSlider(sliderArea, sliderBtn, textLbl, min, max, statName)
	local dragging = false
	sliderBtn.InputBegan:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
			dragging = true
		end
	end)
	UserInputService.InputEnded:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
			dragging = false
		end
	end)
	UserInputService.InputChanged:Connect(function(input)
		if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
			local mouseX = input.Position.X
			local absPos = sliderArea.AbsolutePosition.X
			local absSize = sliderArea.AbsoluteSize.X
			local percent = math.clamp((mouseX - absPos) / absSize, 0, 1)
			
			sliderBtn.Position = UDim2.new(percent, -sliderBtn.Size.X.Offset/2, -0.1, 0)
			local val = math.floor(min + ((max - min) * percent))
			textLbl.Text = tostring(val)
			Values[statName] = val
			
			if statName == "Speed" and States.Speed and humanoid then
				humanoid.WalkSpeed = val
			end
		end
	end)
end

SetupSlider(FlySlider, FlySliderBtn, FlySpeedText, 10, 200, "Fly")
SetupSlider(SpeedSlider, SpeedSliderBtn, SpeedText, 16, 120, "Speed")

-- ===== ЛОГИКА ФУНКЦИЙ =====
local function ToggleFeature(name, state)
	States[name] = state
	local btn, textIcon, activeColor
	if name == "Fly" then btn = FlyBtn; textIcon = "🚀 FLY"; activeColor = Color3.fromRGB(139, 92, 246)
	elseif name == "Noclip" then btn = NoclipBtn; textIcon = "👻 NOCLIP"; activeColor = Color3.fromRGB(59, 130, 246)
	elseif name == "Speed" then btn = SpeedBtn; textIcon = "⚡ SPEED"; activeColor = Color3.fromRGB(245, 158, 11)
	elseif name == "FPS" then btn = BustFPSBtn; textIcon = "🎮 BUST FPS"; activeColor = Color3.fromRGB(16, 185, 129)
	end

	if btn then
		if state then
			btn.Text = textIcon .. " [ВКЛ]"
			btn.TextColor3 = Color3.fromRGB(16, 185, 129)
		else
			btn.Text = textIcon .. " [ВЫКЛ]"
			btn.TextColor3 = Color3.fromRGB(255, 255, 255)
		end
	end

	if FloatingFrames[name] then
		FloatingFrames[name].BackgroundColor3 = state and Color3.fromRGB(16, 185, 129) or activeColor
	end

	pcall(function()
		if name == "Fly" then
			if state then
				if not character or not rootPart or not humanoid then return end
				humanoid.PlatformStand = true
				bg = Instance.new("BodyGyro")
				bg.P = 9e4
				bg.maxTorque = Vector3.new(9e9, 9e9, 9e9)
				bg.cframe = rootPart.CFrame
				bg.Parent = rootPart
				bv = Instance.new("BodyVelocity")
				bv.velocity = Vector3.new(0, 0, 0)
				bv.maxForce = Vector3.new(9e9, 9e9, 9e9)
				bv.Parent = rootPart
			else
				if humanoid then humanoid.PlatformStand = false end
				if bg then bg:Destroy() end
				if bv then bv:Destroy() end
			end
		elseif name == "Noclip" then
			if not state then
				for _, data in pairs(NoclipParts) do
					if data.part and data.part.Parent then
						data.part.CanCollide = data.collide
					end
				end
				NoclipParts = {}
			end
		elseif name == "Speed" then
			if state then
				if humanoid then humanoid.WalkSpeed = Values.Speed end
			else
				if humanoid then humanoid.WalkSpeed = 16 end
			end
		elseif name == "FPS" then
			if state then
				OldLighting.Technology = Lighting.Technology
				OldLighting.GlobalShadows = Lighting.GlobalShadows
				OldLighting.ClockTime = Lighting.ClockTime
				local bloom = Lighting:FindFirstChildOfClass("BloomEffect")
				OldLighting.Bloom = bloom and bloom.Enabled or false
				local atmos = Lighting:FindFirstChildOfClass("Atmosphere")
				OldLighting.Atmosphere = atmos

				pcall(function() Lighting.Technology = Enum.Technology.Compatibility end)
				pcall(function() Lighting.GlobalShadows = false end)
				pcall(function() Lighting.ClockTime = 12 end)
				if bloom then pcall(function() bloom.Enabled = false end) end
				if atmos then pcall(function() atmos.Parent = nil end) end

				task.spawn(function()
					for _, v in pairs(Workspace:GetDescendants()) do
						pcall(function()
							if v:IsA("ParticleEmitter") or v:IsA("Fire") or v:IsA("Smoke") or v:IsA("Sparkles") then
								v.Enabled = false
							elseif v:IsA("Texture") or v:IsA("Decal") then
								v.Texture = ""
							end
						end)
					end
				end)
			else
				pcall(function() Lighting.Technology = OldLighting.Technology or Enum.Technology.ShadowMap end)
				pcall(function() Lighting.GlobalShadows = OldLighting.GlobalShadows or true end)
				pcall(function() Lighting.ClockTime = OldLighting.ClockTime or 14 end)
				local bloom = Lighting:FindFirstChildOfClass("BloomEffect")
				if bloom then pcall(function() bloom.Enabled = OldLighting.Bloom end) end
				if OldLighting.Atmosphere then pcall(function() OldLighting.Atmosphere.Parent = Lighting end) end
			end
		end
	end)
end

FlyBtn.MouseButton1Click:Connect(function() ToggleFeature("Fly", not States.Fly) end)
NoclipBtn.MouseButton1Click:Connect(function() ToggleFeature("Noclip", not States.Noclip) end)
SpeedBtn.MouseButton1Click:Connect(function() ToggleFeature("Speed", not States.Speed) end)
BustFPSBtn.MouseButton1Click:Connect(function() ToggleFeature("FPS", not States.FPS) end)

ResetBtn.MouseButton1Click:Connect(function()
	ToggleFeature("Fly", false)
	ToggleFeature("Noclip", false)
	ToggleFeature("Speed", false)
	ToggleFeature("FPS", false)
	
	FlySliderBtn.Position = UDim2.new((50-10)/(200-10), -10, -0.1, 0)
	FlySpeedText.Text = "50"
	Values.Fly = 50
	
	SpeedSliderBtn.Position = UDim2.new((40-16)/(120-16), -10, -0.1, 0)
	SpeedText.Text = "40"
	Values.Speed = 40
end)

-- ПЛАВАЮЩИЕ КНОПКИ
local function SpawnFloating(name, color, text)
	if FloatingFrames[name] then
		FloatingFrames[name]:Destroy()
		FloatingFrames[name] = nil
		return
	end

	local btn = Instance.new("TextButton")
	btn.Name = "Float"..name
	btn.Size = UDim2.new(0, 65, 0, 65)
	btn.Position = FloatingPositions[name] or UDim2.new(0.5, -32, 0.5, -32)
	btn.BackgroundColor3 = States[name] and Color3.fromRGB(16, 185, 129) or color
	btn.BackgroundTransparency = 0.15
	btn.Text = text
	btn.TextColor3 = Color3.fromRGB(255, 255, 255)
	btn.Font = Enum.Font.GothamBold
	btn.TextSize = 12
	btn.Parent = ScreenGui
	btn.Visible = true
	Instance.new("UICorner", btn).CornerRadius = UDim.new(1, 0)

	local dragging = false
	local dragStart, startPos
	local clickTime = 0

	btn.InputBegan:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
			dragging = true
			dragStart = input.Position
			startPos = btn.Position
			clickTime = tick()
		end
	end)

	UserInputService.InputEnded:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
			if dragging then
				dragging = false
				FloatingPositions[name] = btn.Position
				if tick() - clickTime < 0.2 then
					ToggleFeature(name, not States[name])
				end
			end
		end
	end)

	UserInputService.InputChanged:Connect(function(input)
		if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
			local delta = input.Position - dragStart
			local vpSize = Workspace.CurrentCamera.ViewportSize
			local newX = math.clamp(startPos.X.Offset + delta.X, 0, vpSize.X - 65)
			local newY = math.clamp(startPos.Y.Offset + delta.Y, 0, vpSize.Y - 65)
			btn.Position = UDim2.new(0, newX, 0, newY)
		end
	end)

	FloatingFrames[name] = btn
end

PlusFly.MouseButton1Click:Connect(function() SpawnFloating("Fly", Color3.fromRGB(139, 92, 246), "FLY") end)
PlusNoclip.MouseButton1Click:Connect(function() SpawnFloating("Noclip", Color3.fromRGB(59, 130, 246), "NOCLIP") end)
PlusSpeed.MouseButton1Click:Connect(function() SpawnFloating("Speed", Color3.fromRGB(245, 158, 11), "SPEED") end)

-- РАБОТА FLY (ОБНОВЛЕНИЕ КАМЕРЫ)
RunService.Heartbeat:Connect(function()
	pcall(function()
		if States.Fly and character and humanoid and rootPart and bg and bv then
			local cam = Workspace.CurrentCamera
			local moveVector = Vector3.new(0,0,0)
			local s, ctrl = pcall(function() return require(lplr.PlayerScripts:WaitForChild("PlayerModule"):WaitForChild("ControlModule")) end)
			if s and ctrl then
				moveVector = ctrl:GetMoveVector()
			else
				if UserInputService:IsKeyDown(Enum.KeyCode.W) then moveVector = moveVector + Vector3.new(0,0,-1) end
				if UserInputService:IsKeyDown(Enum.KeyCode.S) then moveVector = moveVector + Vector3.new(0,0,1) end
				if UserInputService:IsKeyDown(Enum.KeyCode.A) then moveVector = moveVector + Vector3.new(-1,0,0) end
				if UserInputService:IsKeyDown(Enum.KeyCode.D) then moveVector = moveVector + Vector3.new(1,0,0) end
			end

			local flyVelocity = Vector3.new(0,0,0)
			if moveVector.Z < 0 then flyVelocity = flyVelocity + cam.CFrame.LookVector end
			if moveVector.Z > 0 then flyVelocity = flyVelocity - cam.CFrame.LookVector end
			if moveVector.X < 0 then flyVelocity = flyVelocity - cam.CFrame.RightVector end
			if moveVector.X > 0 then flyVelocity = flyVelocity + cam.CFrame.RightVector end

			if flyVelocity.Magnitude > 0 then
				bv.velocity = flyVelocity.Unit * Values.Fly
			else
				bv.velocity = Vector3.new(0,0,0)
			end
			bg.cframe = cam.CFrame
		end
	end)
end)

-- РАБОТА NOCLIP
RunService.Stepped:Connect(function()
	pcall(function()
		if States.Noclip and character then
			for _, v in pairs(character:GetDescendants()) do
				if v:IsA("BasePart") then
					if v.CanCollide then
						local found = false
						for _, data in pairs(NoclipParts) do
							if data.part == v then found = true break end
						end
						if not found then table.insert(NoclipParts, {part = v, collide = v.CanCollide}) end
					end
					v.CanCollide = false
				end
			end
		end
	end)
end)

-- ===== ПРОВЕРКА КОЛИЧЕСТВА ЭЛЕМЕНТОВ =====
local childCount = #MainFrame:GetChildren()
print("Создано элементов в меню:", childCount)
print("Ожидалось: 15+ элементов")

if childCount < 15 then
	warn("ВНИМАНИЕ! В меню меньше 15 элементов! Что-то пошло не так.")
else
	print("✅ Меню создано успешно! Все элементы на месте!")
end

print("SOFIYA <3 загружена!")
